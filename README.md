# Unlearning Optimization Methods Comparison

This project compares four optimization methods for machine unlearning in logistic regression:
- Newton Update
- L-BFGS
- Gradient Descent (GD)

## Setup
1. Clone the repository:
   ```bash
   git clone <repo-url>
   cd unlearning_experiments
